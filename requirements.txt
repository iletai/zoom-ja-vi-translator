# Real-time Japanese -> Vietnamese Zoom translator (CPU-only, no GPU required)

# Core numerics
numpy>=1.26,<2

# High-quality anti-aliased audio resampling (loopback 48k -> 16k ASR input).
# Falls back to scipy.resample_poly, then linear, if absent.
soxr>=0.3.7

# Cross-platform audio loopback capture
soundcard>=0.4.3

# Japanese ASR: ReazonSpeech k2 via sherpa-onnx
sherpa-onnx>=1.10.0

# Translation: NLLB-600M via CTranslate2 (4-8x faster than PyTorch on CPU)
ctranslate2>=4.0.0
transformers>=4.48.0
sentencepiece>=0.2.0

# Voice Activity Detection
webrtcvad>=2.0.10

# Model download / conversion helpers
huggingface_hub>=0.23.0

# Optional: progress bars for model download
tqdm>=4.66.0

# LLM Translation: Qwen2.5 via llama.cpp (optional, for higher-quality context-aware translation)
# Install with: pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python>=0.2.90,<0.3.0
